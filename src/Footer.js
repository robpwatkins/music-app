import React from 'react';
import { AppBar, Toolbar } from '@material-ui/core';

function Footer () {
  return (
    <AppBar>
      <Toolbar>
        Heyoo
      </Toolbar>
    </AppBar>
  )
}

export default Footer;